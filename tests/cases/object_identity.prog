def obj_a = {
	hello = "world!"
}

def obj_b = {
	hello = "world!"
}

def obj_alias_a = obj_a
def obj_alias_b = obj_b

assert_eq(obj_a, obj_a)
assert_eq(obj_b, obj_b)

assert_neq(obj_a, obj_b)
assert_neq(obj_alias_a, obj_alias_b)

assert_eq(obj_alias_a, obj_alias_a)
assert_eq(obj_alias_a, obj_a)
assert_neq(obj_alias_a, obj_b)

assert_eq(obj_alias_b, obj_alias_b)
assert_eq(obj_alias_b, obj_b)
assert_neq(obj_alias_b, obj_a)