class File
	def handle

	def new = func(path, mode) do
		def handle = (path, mode) -> extern "file_open"
		("got handle: ", handle) -> print

		return ({
			handle = -1
		}) -> self
	end

	def write = func(data) do
		("write is not yet implemented") -> print
	end

	def close = func() do
		("close is not yet implemented") -> print
	end
end

def fs = {}

fs.File = File

return fs